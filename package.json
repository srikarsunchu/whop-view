{
  "name": "whop-view",
  "version": "0.0.1",
  "description": "wv: a human view layer for the Whop CLI. Unofficial design prototype, not affiliated with Whop.",
  "author": "srikarsunchu",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "wv": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && rm -rf dist/hints && cp -R src/hints dist/hints",
    "dev": "node --experimental-strip-types --no-warnings src/bin.ts",
    "test": "node --test --experimental-strip-types --no-warnings tests/*.test.ts",
    "fixtures": "node --experimental-strip-types --no-warnings scripts/fixtures.ts",
    "demo": "pnpm build && rm -rf demo/*.frames && for t in demo/*.tape; do case $t in *_common*) ;; *) vhs \"$t\";; esac; done && sh scripts/gif.sh",
    "render": "node --experimental-strip-types --no-warnings tests/render.ts",
    "skill": "mkdir -p ~/.claude/skills && rm -rf ~/.claude/skills/whop-gtm && cp -R skills/whop-gtm ~/.claude/skills/whop-gtm && echo installed ~/.claude/skills/whop-gtm"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.14.0"
}
