# wv agent payouts

Send money from a balance to a bank or wallet.

whop@0.18.2 · API 2026-09-15 · generated by wv from `whop payouts <verb> --schema`. The schema is the truth; rerun `wv agent payouts` after `whop upgrade`.

## Writes go through wv

Run reads as `whop payouts <verb> … --format json`. Run every write as `wv payouts <verb> …` instead of `whop`. Without `--yes` wv does not run it: it exits 2 with a JSON envelope whose `error.code` is `CONFIRMATION_REQUIRED`, whose `plan` is what the write commits (account, flags, amount, balance, caps, and for ads the campaign tree, reach, and committed spend), and whose `rerun` is the argv that runs it, carrying an `--approve` token bound to that exact command that expires in ten minutes. Show the plan to the person, then run `rerun` unchanged. `--plan` returns `{ ok: true, plan }` and runs nothing. Refusals come back the same way with no `rerun`: `WHOP_LIMIT` (Whop's own limit, in its words), `WV_CAP` (`WV_PAYOUT_CAP`), `INSUFFICIENT_BALANCE`, `WV_AD_CAP` (`WV_AD_CAP`). `--format json` does not lift the gate. Every write also takes `--idempotency-key`; generate it from the plan step, not the call, so a retry never writes twice.

Before the first write, run `wv doctor --format json` and make sure these checks are `ok`: `identity` (identity). Each failing check carries its `fix` command.

## Object and array flags

`whop` takes objects and arrays as JSON on the command line: `--ad_group '{"budget_amount":40}'`, `--headlines '["a","b"]'`. Through `wv` three more spellings assemble to the same JSON: a dotted path (`--ad_group.budget_amount 40`), a repeated flag for an array (`--headlines a --headlines b`), and `@file.json` for a whole value. Plain JSON is never rewritten.

## Commands

| command | kind | description |
|---|---|---|
| `whop payouts cancel` | write, money, destructive | Cancel Payout |
| `whop payouts create` | write, money | Create Payout |
| `whop payouts create-method` | write, money | Create Saved Payout Method |
| `whop payouts delete-method` | write, money | Delete Saved Payout Method |
| `whop payouts get` | read | Retrieve Payout |
| `whop payouts list` | read | List Payouts |
| `whop payouts methods` | read | List Saved Payout Methods |
| `whop payouts quotes` | read | Create Payout Quote |
| `whop payouts supported-methods` | read | List Supported Payout Methods |
| `whop payouts update-method` | write, money | Update Saved Payout Method |

## payouts cancel · write · money · destructive

Cancel Payout

Arguments: <id> Payout ID, prefixed `wdrl_`, or the `cofr_` payout request ID returned by `POST /payouts` — both cancel the same payout.

| flag | type | required | description |
|---|---|---|---|
| `--account_id` | string | no | Owning account ID, prefixed `biz_`. Provide exactly one of `account_id` or `user_id`. |
| `--user_id` | string | no | Owning user ID, prefixed `user_`. Provide exactly one of `account_id` or `user_id`. |
| `--idempotency-key` | string | no | A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). Example `d9105228-4a08-46b1-8b91-42fed586d383`. |

> Moves money. Run through `wv`; without `--yes` it returns the plan and exits 2.

## payouts create · write · money

Create Payout

| flag | type | required | description |
|---|---|---|---|
| `--account_id` | string | no | Account to pay out from, prefixed `biz_`. Provide exactly one of `account_id` or `user_id`. Example `biz_xxxxxxxxxxxxxx`. |
| `--acknowledge_bank_warning` | boolean | no | Set to `true` to continue when the destination bank could not confirm the payout method account holder's name, or `false` to have the payout refused in that case so the account holder can correct the name or link their bank first. Omitting the field skips the warning gate — a client that cannot show the warning keeps its pre-gate behavior. |
| `--amount` | number | yes | The amount to pay out in the specified currency. |
| `--currency` | string | no | The currency to pay out. Balances are held per currency and the payout draws only from the balance in this currency, so match the currency the funds arrived in — for example `cad` for an account funded by CAD transfers. When omitted, uses `usd` if that balance can cover a withdrawal, otherwise the account's only other funded currency. Example `usd`. |
| `--metadata` | object | no | Key-value data to attach to the payout, echoed on every read and in webhook payloads. At most 50 keys, key names up to 40 characters, string values up to 500 characters. Never store secrets or regulated personal data here — webhook bodies are retained for delivery inspection. Example `{"batch_id":"2026-08-18"}`. |
| `--notes` | string | null | no | Free-form notes to attach to the payout, with a maximum of 255 characters. Omit or pass `null` for no notes. Example `Detailing supplies restock`. |
| `--payout_method_id` | string | yes | The saved payout method to deliver to (a potk_ identifier). Example `potk_xxxxxxxxxxxxxx`. |
| `--platform_covers_fees` | boolean | no | Whether the parent platform covers the payout fee instead of the account being paid out. Omit to use the platform's configured fee coverage policy; pass `false` to opt out of it. `true` is only accepted for accounts that belong to a platform, and requires the platform's policy to cover this payout method's category or a caller authorized to manage the platform's child account fees. |
| `--quote_token` | string | no | The server-signed quote_token returned by POST /payouts/quotes. Required when the ledger account's payout_quote_required is true; a payout without it is refused with the invalid_payout_quote error type. When provided, Whop will not commit a provider payout below the destination amount the quote showed. Example `signed-payout-quote`. |
| `--speed` | string | no | How fast the funds should arrive. `instant` is only accepted when the account and payout method are eligible; otherwise the payout is rejected. One of `standard`, `instant`. |
| `--statement_descriptor` | string | null | no | Text that appears on the recipient's bank statement. Must be 5-22 alphanumeric characters (A-Z, a-z, 0-9). Without a `quote_token`, omit or pass `null` to use the default descriptor. With a `quote_token`, set this value when creating the quote; the payout request may omit it but cannot add or change it. Example `MYCOMPANY`. |
| `--user_id` | string | no | User to pay out from, prefixed `user_`. Provide exactly one of `account_id` or `user_id`. Example `user_xxxxxxxxxxxxxx`. |
| `--idempotency-key` | string | no | A unique key that makes this request safe to retry. See [Idempotent requests](https://docs.whop.com/developer/api/idempotency). Example `d9105228-4a08-46b1-8b91-42fed586d383`. |

> Moves money. Run through `wv`; without `--yes` it returns the plan and exits 2.
> Object and array flags take JSON; through wv, dotted paths, repeated flags, and `@file` also work.

## payouts create-method · write · money

Create Saved Payout Method

No schema: `create-method` is a command group. Run `whop payouts create-method --help`.

## payouts delete-method · write · money

Delete Saved Payout Method

No schema: `delete-method` is a command group. Run `whop payouts delete-method --help`.

## payouts get · read

Retrieve Payout

No schema: `get` is a command group. Run `whop payouts get --help`.

## payouts list · read

List Payouts

| flag | type | required | description |
|---|---|---|---|
| `--account_id` | string | no | The owning account ID (a biz_ identifier). Provide this or user_id. |
| `--user_id` | string | no | The owning user ID (a user_ identifier). Provide this or account_id. |
| `--currency` | string | no | Optional currency code filter, for example `usd`. |
| `--status` | string | no | Filter to payouts whose `status` reads this word, matching exactly what this version displays — `reversed` finds settled payouts the bank later returned. Requires Api-Version-Date 2026-08-21 or later. One of `requested`, `in_review`, `processing`, `completed`, `reversed`, `canceled`, `failed`, `denied`. |
| `--source` | string | no | Filter by how the payout was created. Payouts created before source tracking or through internal tooling carry no source and never match. One of `api`, `dashboard`, `automatic`. |
| `--payout_method_id` | string | no | Filter to payouts sent to one saved payout method (a pytk_ identifier). An unknown id matches nothing. |
| `--created_before` | string | no | Only payouts created before this ISO 8601 time (exclusive). |
| `--created_after` | string | no | Only payouts created at or after this ISO 8601 time (inclusive). |
| `--first` | integer | no | Number of results to return from the start of the range. Default `20`. |
| `--after` | string | no | Return results after this cursor. Use `page_info.end_cursor` from the previous response to fetch the next page. |
| `--last` | number | no | Number of results to return from the end of the range. |
| `--before` | string | no | Return results before this cursor. Use `page_info.start_cursor` from the previous response to fetch the previous page. |

## payouts methods · read

List Saved Payout Methods

| flag | type | required | description |
|---|---|---|---|
| `--account_id` | string | no | The owning account ID (a biz_ identifier). Provide this or user_id. |
| `--user_id` | string | no | The owning user ID (a user_ identifier). Provide this or account_id. |
| `--status` | string | no | Optional status filter. `created` means saved but unused, `active` means a payout through it succeeded, `broken` means the last payout failed and the method needs fixing. One of `created`, `active`, `broken`. |
| `--amount` | number | no | Optional payout amount in whole currency units, for example `250.00`. When provided, each method includes a quote with the estimated fee, amount received, and delivery date for that amount. |
| `--currency` | string | no | Currency code of the amount, for example `usd`. Only meaningful with amount or include_limits. Default `"usd"`. |
| `--include_limits` | boolean | no | When true, the response also carries limits — the live per-speed payout caps the account's payout requests are validated against, in the requested currency. Requires the payout:withdrawal:read scope. Default `false`. |
| `--first` | integer | no | Number of results to return from the start of the range. Default `20`. |
| `--after` | string | no | Return results after this cursor. Use `page_info.end_cursor` from the previous response to fetch the next page. |
| `--last` | number | no | Number of results to return from the end of the range. |
| `--before` | string | no | Return results before this cursor. Use `page_info.start_cursor` from the previous response to fetch the previous page. |

## payouts quotes · read

Create Payout Quote

No schema: `quotes` is a command group. Run `whop payouts quotes --help`.

## payouts supported-methods · read

List Supported Payout Methods

No schema: `supported-methods` is a command group. Run `whop payouts supported-methods --help`.

## payouts update-method · write · money

Update Saved Payout Method

No schema: `update-method` is a command group. Run `whop payouts update-method --help`.

